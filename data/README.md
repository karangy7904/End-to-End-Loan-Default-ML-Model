# Data

`Training Data.csv` contains 252,000 labeled rows. `Test Data.csv` contains
28,000 unlabeled rows. The target is `Risk_Flag`; `Id` is retained only for
joining predictions and is excluded from model features.

