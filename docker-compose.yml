services:
  api:
    build: .
    ports:
      - "8000:8000"
    command: ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
  gradio:
    build: .
    ports:
      - "7860:7860"
    command: ["python", "gradio_app.py"]

