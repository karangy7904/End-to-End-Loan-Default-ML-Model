# Notebook guide

The reproducible analysis is implemented as `src/train.py` so it runs the same
way locally, in CI, and from a notebook. To explore interactively, import the
functions from `src.train` into a notebook and load the CSV files from `data/`.

